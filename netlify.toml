# Static deploy only.
# Keep Netlify clean URLs explicit so canonical, sitemap, and navigation URLs agree.

[build.processing.html]
  pretty_urls = true

[[redirects]]
from = "/warning-sign.mp4"
to = "/warning-hero.mp4"
status = 200
